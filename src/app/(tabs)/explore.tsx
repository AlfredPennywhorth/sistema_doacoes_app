import { View } from 'react-native';

// Esta tela é um resíduo do template Expo e não aparece na navegação.
// Mantida vazia para evitar erros de bundling.
export default function ExploreScreen() {
  return <View />;
}
